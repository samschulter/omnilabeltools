# File only exists for editable install of the library. Otherwise, we need a version requirement >=64.0.0 for setuptools which implements PEP 660 (https://github.com/pypa/setuptools/issues/2816, https://setuptools.pypa.io/en/latest/history.html#id91)
